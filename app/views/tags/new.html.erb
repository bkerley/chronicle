<h1>New tag</h1>

<%= error_messages_for :tag %>

<% form_for(@tag) do |f| %>
  <p>
		<%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', tags_path %>
